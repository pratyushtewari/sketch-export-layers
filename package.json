{
  "name": "export-group-to-html",
  "version": "0.1.0",
  "engines": {
    "sketch": ">=3.0"
  },
  "skpm": {
    "name": "export-group-to-html",
    "manifest": "src/manifest.json",
    "main": "export-group-to-html.sketchplugin",
    "assets": [
      "assets/**/*"
    ]
  },
  "scripts": {
    "build": "skpm-build",
    "watch": "skpm-build --watch",
    "start": "skpm-build --watch --run",
    "postinstall": "npm run build && skpm-link"
  },
  "devDependencies": {
    "@skpm/builder": "^0.5.11"
  },
  "author": "Pratyush Tewari <pratyush.tewari@chase.com>",
  "dependencies": {
    "json-to-css": "^0.1.0"
  }
}
